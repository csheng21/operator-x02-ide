// src/types/systemInfo.ts
export interface SystemInfo {
  username: string;
  hostname: string;
  platform: string;
  platformVersion: string;
  arch: string;
  homeDir: string;
  documentsDir: string;
  downloadsDir: string;
}